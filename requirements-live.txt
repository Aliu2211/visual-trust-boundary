# Live webcam capture only (decode.capture live mode). Not needed for replay experiments,
# so it stays off the measurement path and out of the laptop dev image.
-r requirements.txt
opencv-python-headless>=4.8
